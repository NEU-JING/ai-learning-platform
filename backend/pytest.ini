# pytest configuration for AILP
# SDD: ci_only marker — tests requiring Docker/GPU/large memory, skipped locally
[pytest]
markers =
    ci_only: 仅在 CI 环境中运行的测试（需要 Docker/GPU/大内存等资源）
testpaths = tests
