# .hermes.md — AILP（AI 辅助学习平台）

> 本文件是 Hermes Agent（小爱）在本项目开发时的入口。
> 通用 Agent 规范见 AGENTS.md，项目宪法见 CONSTITUTION.md。

## 进入项目首先读取

1. `CONSTITUTION.md` — 不可违反的核心原则
2. `AGENTS.md` — SDD 开发流程 + 多 Agent 协作
3. `QUIRKS.md` — 已知陷阱

## 关键约束摘要

- TDD 强制：先写测试后写实现
- Pydantic Schema 为唯一真相源，前后端字段名必须一致
- 修改前端必须通过 Playwright 冒烟测试
- 数据契约不可违反：启动断言 + 16 项 CI 门禁

## 快速命令

```bash
cd backend
source /tmp/ailp-venv/bin/activate
SERVE_STATIC=True uvicorn app.main:app --host 0.0.0.0 --port 8000
pytest tests/ -v
pytest tests/test_data_contract.py -v
```
